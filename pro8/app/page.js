// "use client"
// import { useState, useEffect } from "react"

export default function Home() {
  // const [count, setCount]= useState(0)
  return (
    <div>
      I am component in Home with value&nbsp;
      {/* {count}
      <br />
      <button onClick={()=>{setCount(count+1)}}>Increase</button> */}
    </div>
  );
}
